node_modules/
*.log
.DS_Store
coverage/

# Local tooling / editor state
.claude/

# Genuinely-real captures must never be committed (synthetic-only repo, see docs/design.md §9.2).
# Synthetic captures (e.g. test0.csv from beacon-emitter.html) ARE committed.
/*.real.csv
/*.local.csv
